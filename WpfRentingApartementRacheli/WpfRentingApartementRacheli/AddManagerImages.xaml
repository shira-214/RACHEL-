<Page x:Class="WpfRentingApartementRacheli.AddManagerImages"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="הוספת/עריכת תמונה"
      FlowDirection="RightToLeft">

    <Grid Margin="20" Background="#FDFBF7">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="450">
            <TextBlock x:Name="tbTitle" Text="הוספת תמונה לדירה" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
            <TextBlock Text="בחר דירה" Margin="0,0,0,5"/>
            <ComboBox x:Name="cmbApartment" DisplayMemberPath="NumberHouse" Height="35" Margin="0,0,0,15"
                      SelectionChanged="cmbApartment_SelectionChanged"/>

            <Border Height="180" BorderBrush="#DDD" BorderThickness="1" Margin="0,0,0,15">
                <Image x:Name="imagePreview" Stretch="Uniform"/>
            </Border>

            <CheckBox x:Name="chkStatus" Content="תמונה פעילה" IsChecked="True" Margin="0,0,0,15"/>

            <Button Content="בחר תמונה" Height="35" Margin="0,0,0,10" Click="btnSelect_Click"/>
            <Button Cursor="Hand" x:Name="btnEnd" Content="שמור תמונה" Height="35" Margin="0,0,0,10" Click="btnEnd_Click"/>
            <Button Content="הוסף תמונה נוספת" Height="35" Margin="0,0,0,10" Click="btnAddAnother_Click"/>

            <TextBlock Text="תמונות קיימות" FontSize="16" FontWeight="Bold" Margin="0,5,0,5"/>
            <ListView x:Name="lvExistingImages" Height="120" Margin="0,0,0,10">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="מספר" DisplayMemberBinding="{Binding NumImage}" Width="60"/>
                        <GridViewColumn Header="פעילה" DisplayMemberBinding="{Binding Stataus}" Width="60"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
