<Page x:Class="WpfRentingApartementRacheli.MyRentingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ההזמנות שלי"
      FlowDirection="RightToLeft">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
    <Grid Margin="20" Background="#FDFBF7">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="ההזמנות שלי" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>

        <DataGrid x:Name="dgRentings" Grid.Row="1" AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserAddRows="False" MinHeight="200">
            <DataGrid.Columns>
                <DataGridTextColumn Header="תאריך" Binding="{Binding Date, StringFormat={}{0:dd/MM/yyyy}}" Width="100"/>
                <DataGridTextColumn Header="עיר" Binding="{Binding KodHapartment.IdCities.NameCity}" Width="120"/>
                <DataGridTextColumn Header="רחוב" Binding="{Binding KodHapartment.IdStreet.StreetName}" Width="120"/>
                <DataGridTextColumn Header="מחיר" Binding="{Binding SumPayment}" Width="80"/>
                <DataGridTextColumn Header="מיטות" Binding="{Binding SumBeds}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" Content="חזרה לחיפוש" Width="140" HorizontalAlignment="Right"
                Margin="0,15,0,0" Click="btnBack_Click"/>
    </Grid>
    </ScrollViewer>
</Page>
