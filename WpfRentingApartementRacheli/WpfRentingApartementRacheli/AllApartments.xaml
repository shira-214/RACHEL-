<Page x:Class="WpfRentingApartementRacheli.AllApartments"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfRentingApartementRacheli"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="����� �����">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.3*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Content="���� ����" Margin="3,0" Click="Button_Click" />
            <Button Content="����� ����" Margin="3,0" Click="Button_Click_1"/>
            <Button Content="����� ����" Margin="3,0" Click="Button_Click_2"/>

        </StackPanel>
        <DataGrid x:Name="dg1" Grid.Row="1"/>
        <ListView x:Name="lvApartment" Background="White" Grid.Row="1" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="��� ����" DisplayMemberBinding="{Binding IdApartment}"/>
                    <GridViewColumn Header="�� �����" DisplayMemberBinding="{Binding NameOwner}"/>
                    <GridViewColumn Header="��� ����" DisplayMemberBinding="{Binding IdStreet}"/>
                    <GridViewColumn Header="���� ���" DisplayMemberBinding="{Binding NumberHouse}"/>
                    <GridViewColumn Header="����" DisplayMemberBinding="{Binding Floor}"/>
                    <GridViewColumn Header="���� �����" DisplayMemberBinding="{Binding NumberRooms}"/>
                    <GridViewColumn Header="���� �����" DisplayMemberBinding="{Binding NumberBeds}"/>
                    <GridViewColumn Header="���� �������" DisplayMemberBinding="{Binding MinimumPrice}"/>
                    <GridViewColumn Header="����� �����" DisplayMemberBinding="{Binding ExtraForBed}"/>
                    <GridViewColumn Header="�����" DisplayMemberBinding="{Binding Status}"/>
                    <GridViewColumn Header="�����" DisplayMemberBinding="{Binding note}"/>
                    <GridViewColumn Header="��� ��� " DisplayMemberBinding="{Binding DTOCities.NameCity}"/>
                    <GridViewColumn Header="����� ��� �����" DisplayMemberBinding="{Binding PhoneOwner}"/>

                </GridView>
            </ListView.View>
        </ListView>


    </Grid>
</Page>
