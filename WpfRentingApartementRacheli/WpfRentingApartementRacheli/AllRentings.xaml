<Page x:Class="WpfRentingApartementRacheli.AllRentings"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfRentingApartementRacheli"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="����� ������">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.3*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Content="����� ����" Margin="3,0" Click="Button_Click"/>
            <Button Content="����� �����" Margin="3,0" Click="Button_Click_1"/>
            <Button Content="����� �����" Click="Button_Click_2"/>
        </StackPanel>
        <DataGrid x:Name="dg1" Grid.Row="1"/>
        <ListView x:Name="lvRentings" Background="White" Grid.Row="1" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="��� �����" DisplayMemberBinding="{Binding IdRenting}"/>
                    <GridViewColumn Header="����� ����" DisplayMemberBinding="{Binding IdHirer.FhoneNumberHirer}"/>
                    <GridViewColumn Header="���� ������" DisplayMemberBinding="{Binding SumPayment}"/>
                    <GridViewColumn Header="�����" DisplayMemberBinding="{Binding Date, StringFormat={}{0:dd/MM/yyyy}}"/>
                    <GridViewColumn Header="���� �����" DisplayMemberBinding="{Binding SumBeds}"/>
                    <GridViewColumn Header="����" DisplayMemberBinding="{Binding KodHapartment.IdStreet.StreetName}"/>
                    <GridViewColumn Header="���� ���" DisplayMemberBinding="{Binding KodHapartment.NumberHouse}"/>
                    <GridViewColumn Header="���" DisplayMemberBinding="{Binding KodHapartment.IdCities.NameCity}"/>





                </GridView>
            </ListView.View>
        </ListView>


    </Grid>
</Page>
